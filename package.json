{
  "name": "isu2",
  "version": "0.0.0",
  "description": "An improved version of my ISU project",
  "main": "",
  "scripts": {
    "build:watch": "watch -p '*.js' -c 'npm run build'",
    "build": "babel *.js --ignore out.js --presets env | browserify - | babel --presets env > out.js",
    "build:prod": "babel *.js --ignore out.js --presets env | browserify - | babel --presets env | uglifyjs >out.js"
  },
  "author": "thatlittlegit",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "browserify": "^14.4.0",
    "uglify-js": "^3.1.3",
    "watch-cli": "^0.2.2"
  },
  "dependencies": {
    "bulma": "^0.5.3",
    "dom-loaded": "^1.0.1",
    "moonjs": "^0.11.0"
  }
}
